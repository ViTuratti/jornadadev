1. Str() ? Fun‡Æo
Justificativa: converte um n£mero em uma string e retorna esse valor.

2. QOut() ? Procedimento
Justificativa: apenas exibe um texto na tela, sem retornar um valor.

3. Date() ? Fun‡Æo
Justificativa: retorna a data do sistema.

4. Len() ? Fun‡Æo
Justificativa: retorna o tamanho.

5. Upper() ? Fun‡Æo
Justificativa: retorna a string com todos os caracteres em mai£sculas.

6. ClearScreen() ? Procedimento
Justificativa: apenas limpa a tela, sem retornar nenhum valor.